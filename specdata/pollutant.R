
pollutantmean<-function(directory,pollutant,id=1:332){
	directory<-getwd()
		pollutant<-data$nitrate
		id=data$ID
			data <- do.call("rbind", lapply(list.files("directory"),
				read.csv, header=TRUE))
	mean(pollutant, na.rm=TRUE)
}
