
pollutantmean<-function(directory,pollutant,id=1:332){
	directory<-getwd()
		pollutant<-data$nitrate/data$sulfate
			data <- do.call("rbind", lapply( list.files("d:/CHS/TB Program/Ref/R Programming/specdata),
				read.csv, header=TRUE))
	mean(pollutant)
}
